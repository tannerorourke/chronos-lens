# Lambda Cloud A100  CUDA 12.4
# uv:  uv venv && source .venv/bin/activate && uv pip install -r requirements.txt
# pip: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Run both commands from the chronos-lens/ directory.

--index-url https://download.pytorch.org/whl/cu124
--extra-index-url https://pypi.org/simple

# --- Core ---
torch==2.6.0+cu124
numpy>=1.26.0,<3
pandas>=2.2.0
scikit-learn>=1.4.0
scipy>=1.13.0
tqdm>=4.66.0
PyYAML>=6.0.1

# --- Visualization / analysis ---
matplotlib>=3.9.0
umap-learn>=0.5.7
phate>=1.0.11
pingouin>=0.5.4
hdbscan>=0.8.38.post1
xgboost>=2.1.0

# --- Logging ---
tensorboard>=2.18.0

# --- Google Cloud (MIMIC extraction; not needed for training-only runs) ---
google-auth>=2.30.0
google-cloud-bigquery>=3.20.0
db-dtypes>=1.3.0
pyarrow>=16.0.0

# --- Package (editable; run from chronos-lens/) ---
-e .
