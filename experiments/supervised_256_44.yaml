# supervised_256_44.yaml  (embed_dim 256, supervised, seed 44) - generated from plan §1/§5
model:
    embed_dim: 256
    encoder_heads: 8
    encoder_depth: 4
    encoder_ffn_dim: 1024        # ~4x embed_dim
    token_enc_heads: 2
    token_enc_depth: 2
    token_enc_ffn_dim: 512      # ~2x embed_dim
    architecture: supervised
optimization:
    epochs: 75
    warmup_epochs: 7     # ~10% of optimizer steps
    patience: 15
    base_lr: 3e-4
    min_lr: 1e-5
    weight_decay: 0.05
    betas: [0.9, 0.95]
    grad_clip: 2.0
data:
    label_key: "label_escalation"
    batch_size: 64
    accumulation_steps: 3
    n_patients: 0
    max_encounters: 20
    pin_mem: true
    num_workers: 0
meta:
    seed: 44
    use_bfloat16: True
    label_key: "label_escalation"
    tag: "supervised_256"
    description: "Supervised transformer baseline, embed_dim 256, seed 44 (label_escalation)"
    resume_from: null
    save_cycle: 5
    sync_s3: false     # flip true on the Lambda node (see plan §7)
sae_config:
    z_enc:
        n_features: 1024      # ~4x embed_dim
        top_k: 32
        epochs: 30
        lr: 1e-3
        batch_size: 512
